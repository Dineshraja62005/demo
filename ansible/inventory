[jenkins]
jenkins_server ansible_host= ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/my-key-pair.pem

# Note: Replace  with the actual IP address of your Jenkins EC2 instance
# Replace the key file path with your actual key path
